import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
    appId: 'com.synatra.ogre',
    appName: 'ogre',
    webDir: 'build',
    bundledWebRuntime: false
}

export default config
